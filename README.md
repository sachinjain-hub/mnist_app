# 🧠 MNIST Digit Classifier (ANN + Gradio)

This project is a simple **handwritten digit classifier** built using  
**TensorFlow (ANN model)** and deployed with a **Gradio Web App**.

You can upload an image or draw a digit (0–9), and the model will predict the number.

---

## 🚀 Features
- Trained on the **MNIST handwritten digits dataset**
- Uses a simple **Artificial Neural Network (ANN)**
- Interactive **Gradio web interface**
- Accepts:
  - Uploaded images
  - Drawn digits
- Works locally and can be deployed on Render / HuggingFace

---

## 🧩 Project Structure

